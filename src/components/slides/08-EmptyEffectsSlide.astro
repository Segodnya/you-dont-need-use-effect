---
import BaseSlide from './BaseSlide.astro';
import CodeComparison from '../CodeComparison.astro';
import Text from '../ui/Text.astro';
import Callout from '../ui/Callout.astro';
import Section from '../ui/Section.astro';
---

<BaseSlide id="empty-effects" title="1. Пустые эффекты">
  <Section>
    <Text>
      Если эффект ничего не делает - он не нужен.
    </Text>

    <CodeComparison
      badCode={`function Component() {
  useEffect(() => {}, []); // 🤡 Бессмысленный эффект
}`}
      goodCode={`function Component() {
  // Просто удалите его!
}`}
      language="tsx"
    />

    <Callout variant="error">
      Пустой эффект не выполняет никакой полезной работы и только добавляет шум в код.
    </Callout>
  </Section>
</BaseSlide>

---
import BaseSlide from './BaseSlide.astro';
import CodeComparison from '../CodeComparison.astro';
import Text from '../ui/Text.astro';
import Callout from '../ui/Callout.astro';
import Section from '../ui/Section.astro';
---

<BaseSlide id="prop-only-effects" title="2. Эффекты, которые используют только пропсы">
  <Section>
    <Text>
      Если эффект использует только пропсы и не взаимодействует с внешними системами - скорее всего, эта логика должна быть в родительском компоненте.
    </Text>

    <CodeComparison
      badCode={`function Component({ foo }) {
  useEffect(() => {
    console.log(foo);
  }, [foo]);
}`}
      goodCode={`// Поднимите логику на уровень выше
function ParentComponent() {
  const foo = someValue;
  console.log(foo); // Логика в родителе
  
  return <Component foo={foo} />;
}`}
      language="tsx"
    />

    <Callout variant="tip">
      Поднимайте логику на уровень выше, где находятся данные.
    </Callout>
  </Section>
</BaseSlide>

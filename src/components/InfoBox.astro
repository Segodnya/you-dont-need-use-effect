---
export interface Props {
  type?: 'info' | 'warning' | 'success' | 'error' | 'tip';
  title?: string;
  className?: string;
}

const { type = 'info', title, className = '' } = Astro.props;

const typeStyles = {
  info: {
    bg: 'border-2 border-blue-400/40 bg-blue-950/40 backdrop-blur-sm shadow-lg shadow-blue-500/10',
    border: '',
    icon: '💡',
    textColor: 'text-blue-200',
    titleColor: 'text-blue-300',
    background: 'from-blue-500/20 via-purple-500/20 to-cyan-500/20',
  },
  warning: {
    bg: 'border-2 border-yellow-400/40 bg-yellow-950/40 backdrop-blur-sm shadow-lg shadow-yellow-500/10',
    border: '',
    icon: '⚠️',
    textColor: 'text-yellow-200',
    titleColor: 'text-yellow-300',
    background: 'from-yellow-500/20 via-amber-500/20 to-orange-500/20',
  },
  success: {
    bg: 'border-2 border-green-400/40 bg-green-950/40 backdrop-blur-sm shadow-lg shadow-green-500/10',
    border: '',
    icon: '✅',
    textColor: 'text-green-200',
    titleColor: 'text-green-300',
    background: 'from-green-500/20 via-emerald-500/20 to-teal-500/20',
  },
  error: {
    bg: 'border-2 border-red-400/40 bg-red-950/40 backdrop-blur-sm shadow-lg shadow-red-500/10',
    border: '',
    icon: '❌',
    textColor: 'text-red-200',
    titleColor: 'text-red-300',
    background: 'from-red-500/20 via-rose-500/20 to-pink-500/20',
  },
  tip: {
    bg: 'border-2 border-purple-400/40 bg-purple-950/40 backdrop-blur-sm shadow-lg shadow-purple-500/10',
    border: '',
    icon: '💡',
    textColor: 'text-purple-200',
    titleColor: 'text-purple-300',
    background: 'from-purple-500/20 via-violet-500/20 to-indigo-500/20',
  },
};

const style = typeStyles[type];
---

<div class={`relative rounded-xl p-4 lg:p-6 ${className}`}>
  {/* Blurry background */}
  <div class={`absolute inset-0 bg-linear-to-br ${style.background} rounded-xl blur-sm transform scale-110`}></div>
  
  {/* Card content */}
  <div class={`relative ${style.bg} rounded-xl`}>
    <div class="flex items-start gap-3">
      <span class="text-2xl shrink-0">{style.icon}</span>
      <div class="flex-1">
        {
          title && (
            <h4 class={`font-semibold mb-2 ${style.titleColor}`}>{title}</h4>
          )
        }
        <div class={style.textColor}>
          <slot />
        </div>
      </div>
    </div>
  </div>
</div>

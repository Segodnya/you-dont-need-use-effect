---
export interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<h2 class={`text-2xl lg:text-4xl font-black mb-8 bg-linear-to-r from-blue-400 to-cyan-400 bg-clip-text text-transparent ${className}`}>
  <slot />
</h2>

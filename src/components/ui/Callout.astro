---
export interface Props {
  variant?: 'info' | 'warning' | 'error' | 'success' | 'tip' | 'primary';
  icon?: string;
  class?: string;
}

const { variant = 'info', icon, class: className = '' } = Astro.props;

const variants = {
  info: {
    container: 'bg-blue-900/20 border border-blue-700',
    text: 'text-blue-200',
    icon: icon || '💡',
  },
  warning: {
    container: 'bg-yellow-900/20 border border-yellow-700',
    text: 'text-yellow-200',
    icon: icon || '⚠️',
  },
  error: {
    container: 'bg-red-900/20 border border-red-700',
    text: 'text-red-200',
    icon: icon || '❌',
  },
  success: {
    container: 'bg-green-900/20 border border-green-700',
    text: 'text-green-200',
    icon: icon || '✓',
  },
  tip: {
    container: 'bg-yellow-900/20 border border-yellow-700',
    text: 'text-yellow-200',
    icon: icon || '💡',
  },
  primary: {
    container: 'bg-purple-900/30 border border-purple-500',
    text: 'text-purple-200',
    icon: icon || '🔑',
  },
};

const config = variants[variant];
---

<div class={`${config.container} rounded-lg p-4 ${className}`}>
  <div class={`${config.text} flex items-center gap-3`}>
    <span class="text-2xl shrink-0">{config.icon}</span>
    <div class="flex-1"><slot /></div>
  </div>
</div>

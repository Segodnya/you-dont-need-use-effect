---
export interface Props {
  icon?: string;
  iconColor?: string;
  class?: string;
}

const { icon = '✓', iconColor = 'text-green-400', class: className = '' } = Astro.props;
---

<li class={`flex items-start gap-2 ${className}`}>
  <span class={iconColor}>{icon}</span>
  <span><slot /></span>
</li>
